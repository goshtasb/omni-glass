[package]
name = "ocr-bench"
version = "0.2.0"
edition = "2021"
description = "OCR benchmark CLI for Omni-Glass — Apple Vision Framework via swift-bridge FFI"
license = "MIT"

[dependencies]
swift-bridge = "0.1"

[build-dependencies]
swift-bridge-build = "0.1"
